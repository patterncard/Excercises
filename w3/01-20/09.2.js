const dzisiaj = new Date();
const rokSwiat = 2018;
console.log(funkcjaKtoraLiczyDniOdDzisiajDoJakichsSwiat(dzisiaj, rokSwiat));

function funkcjaKtoraLiczyDniOdDzisiajDoJakichsSwiat(oqweasf, kjoadfgo) {
  const toChristmas = new Date(kjoadfgo, 11, 24);
  const msec = toChristmas - oqweasf;
  const days = oijgpoksdfgt(msec);
  return Number(days.toFixed(4));
}

function oijgpoksdfgt(dssdgh) {
  return dssdgh / 1000 / 60 / 60 / 24;
}
